<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /radhacomputech/

  # Only rewrite if the file or directory doesn't exist
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
